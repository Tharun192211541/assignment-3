#include<stdio.h>
void prime()
{
	int i,j,n,a,count;
	printf("enter the range");
	scanf("%d%d",&a,&n);
	for(i=a;i<=n;i++)
	{
		count=0;
		for(j=1;j<=i;j++)
		{
			if(i%j==0)
			{
			count++;
	    	}
		}
		if(count==2)
		printf("%d, ",i);
	}
}
int main()
{
	prime();
}
